#pragma once
#include "constants.h"


template<typename T>T MaxValueIn(T arr[], const int n);	//¬озвращает максимальное значение в массиве;
template <typename T>T MaxValueIn(T arr[ROWS][COLS], const int ROWS, const int COLS);